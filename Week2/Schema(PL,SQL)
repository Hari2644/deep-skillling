CREATE TABLE Customer (
    CustomerID   NUMBER PRIMARY KEY,
    Name         VARCHAR2(100),
    DOB          DATE,
    Balance      NUMBER,
    LastModified DATE
);
CREATE TABLE Account (
    AccountID    NUMBER PRIMARY KEY,
    CustomerID   NUMBER,
    AccountType  VARCHAR2(20),
    Balance      NUMBER,
    LastModified DATE,
    FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID)
);
CREATE TABLE Transaction (
    TransactionID   NUMBER PRIMARY KEY,
    AccountID       NUMBER,
    TransactionDate DATE,
    Amount          NUMBER,
    TransactionType VARCHAR2(10),
    FOREIGN KEY (AccountID) REFERENCES Account(AccountID)
);
CREATE TABLE Loan (
    LoanID       NUMBER PRIMARY KEY,
    CustomerID  NUMBER,
    LoanAmount  NUMBER,
    InterestRate NUMBER,
    StartDate    DATE,
    EndDate      DATE,
    FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID)
);
CREATE TABLE Employee (
    EmployeeID  NUMBER PRIMARY KEY,
    Name        VARCHAR2(100),
    Position    VARCHAR2(50),
    Salary      NUMBER,
    Department  VARCHAR2(50),
    HireDate    DATE
);

INSERT INTO Customer (CustomerID, Name, DOB, Balance, LastModified)
VALUES (1, 'Ravi Kumar', TO_DATE('1950-06-01', 'YYYY-MM-DD'), 12000, SYSDATE);

INSERT INTO Customer (CustomerID, Name, DOB, Balance, LastModified)
VALUES (2, 'Anita Mehra', TO_DATE('1985-09-15', 'YYYY-MM-DD'), 9000, SYSDATE);

-- Insert loans
INSERT INTO Loan (LoanID, CustomerID, LoanAmount, InterestRate, StartDate, EndDate)
VALUES (101, 1, 50000, 9.5, SYSDATE - 100, SYSDATE + 200);

INSERT INTO Loan (LoanID, CustomerID, LoanAmount, InterestRate, StartDate, EndDate)
VALUES (102, 2, 30000, 10.0, SYSDATE - 50, SYSDATE + 150);

COMMIT;

